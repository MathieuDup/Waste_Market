
<h2>This is the product index</h2>

<div class="container">
    <div class="cards">
      <% @products.each do |product| %>
        <%= render 'products/card_product', product: product %>
      <% end %>
    </div>
</div>
