<% if @orders.length > 0 %>
<h2>My conversations with my customers</h2>
<% end %>
<div class="cards-orders">
  <% @orders.each do |order| %>
    <%= render 'orders/card_order', order: order %>
  <% end %>
</div>
