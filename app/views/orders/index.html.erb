<% if @orders.length > 0 %>
<h2>My <%= @orders.length %> orders as buyer</h2>
<% end %>
<div class="cards-orders">
  <% @orders.each do |order| %>
    <%= render 'orders/card_order', order: order %>
  <% end %>
</div>
